# ── Core Flask backend ──────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
flask-sqlalchemy==3.1.1
flask-jwt-extended==4.6.0
flask-compress==1.14
flask-limiter==3.5.0
bcrypt==4.1.3
python-dotenv==1.0.1
requests==2.32.3
Pillow>=10.0.0
gunicorn>=22.0.0
psycopg2-binary>=2.9.9

# ── Moondream2 fallback (optional — only needed if Ollama is not running) ───
# Uncomment these lines ONLY if you want the local Moondream2 fallback.
# They add ~3 GB to your environment. Ollama is preferred instead.
# transformers
# einops
# torch
# torchvision
